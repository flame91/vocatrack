---
description: Export words (csv, anki, json, md).
argument-hint: "[--format=csv|anki|json|md] [--status=active|mastered|archived|all] [--lang=en|ja|ko|all]"
---

> ⚠ Paste script stdout verbatim as a fenced code block — UI hides Bash output.

Run:
```
bash ${CLAUDE_PLUGIN_ROOT}/scripts/export.sh $ARGUMENTS
```
