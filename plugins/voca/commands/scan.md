---
description: Scan the current session transcript for candidate vocabulary words (async background extraction).
argument-hint: "[--status]"
---

User input: `$ARGUMENTS`

Read `${CLAUDE_PLUGIN_ROOT}/skills/voca/SKILL.md` and execute the **Scan workflow** section.

If `$ARGUMENTS` contains `--status`, execute the **Scan status** subsection instead.
