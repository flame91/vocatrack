---
description: Interactive review of unrated active words (AskUserQuestion-driven rating UI).
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/vocab/SKILL.md` and execute the **Review** section.

Up to 4 words at a time, each rendered as one AskUserQuestion question with options `memorized` / `learning` / `unsure`. After submission, run `rate.sh` for each rated word and ask whether to continue with the next batch.
