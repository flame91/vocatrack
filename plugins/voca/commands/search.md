---
description: Case-insensitive search across word/meaning/example/context.
argument-hint: "<query>"
---

> ⚠ Paste script stdout verbatim as a fenced code block — UI hides Bash output.

Run:
```
bash ${CLAUDE_PLUGIN_ROOT}/scripts/search.sh $ARGUMENTS
```
